# Cline Project Rules

## 代码修改后强制审查流程

每次完成代码修改后，**必须**执行以下验证步骤，确保代码质量：

### 步骤 1: 语法/编译检查
- C# 项目：使用 `dotnet build` 或检查 Unity 编译输出
- 检查编译错误、引用错误、类型不匹配、参数数量不匹配等

### 步骤 2: 代码审查
- 检查代码逻辑是否正确
- 检查 API 使用是否符合项目规范
- 检查命名规范、代码风格

### 步骤 3: 修复并重新验证
- 如果发现错误，立即修复
- 修复后重复步骤 1-2，直到没有任何错误
- **禁止**在有编译错误或明显逻辑错误的情况下结束任务

---

## Available Skills

### Code Review
- **Command**: `/code-review`
- **Description**: 代码审查技能，审查未提交代码或指定模块
- **Usage**: 
  - `/code-review` - 审查未提交的代码
  - `/code-review <模块名>` - 审查指定模块（如 AI, Game, MVC 等）

当用户输入 `/code-review` 时，请执行以下操作：

1. 如果没有参数：
   - 执行 `git status` 查看变更文件
   - 执行 `git diff` 获取具体变更内容
   - 对变更进行代码审查

2. 如果有参数（模块名）：
   - 确认模块名称是否有效
   - 扫描 Assets/Scripts/<模块名>/ 目录下的所有 .cs 文件
   - 对模块代码进行全面审查

## 审查标准

### 代码质量检查项

1. **命名规范**
   - 类名使用 PascalCase
   - 方法名使用 PascalCase
   - 私有字段使用 _camelCase 或 m_camelCase
   - 常量使用 UPPER_CASE 或 PascalCase
   - 避免使用缩写，除非是通用的（如 UI, ID）

2. **代码结构**
   - 单一职责原则
   - 方法长度适中（建议不超过50行）
   - 类长度适中（建议不超过500行）
   - 避免深层嵌套（建议不超过3层）

3. **Unity 特定规范**
   - 避免 Update() 中的 Find/GetComponent 调用
   - 使用 SerializeField 而非 public 字段
   - 正确使用 MonoBehaviour 生命周期
   - 避免在循环中分配内存

4. **性能考虑**
   - 避免不必要的字符串拼接
   - 使用 StringBuilder 处理大量字符串操作
   - 注意装箱拆箱操作
   - 合理使用对象池

5. **安全性**
   - 空值检查
   - 数组边界检查
   - 异常处理

6. **可维护性**
   - 适当的注释
   - 代码可读性
   - 避免魔法数字
   - 合理的代码组织

## 输出格式

审查报告按以下格式输出：

```markdown
# 代码审查报告

## 审查范围
- **模式**: [未提交代码/指定模块]
- **目标**: [变更文件列表 或 模块名]

## 问题统计
| 严重程度 | 数量 |
|---------|------|
| 🔴 严重  | X    |
| 🟡 中等  | X    |
| 🟢 建议  | X    |

## 详细问题

### 🔴 严重问题
1. **文件**: `文件路径`
   - **行号**: X
   - **问题**: 问题描述
   - **建议**: 修复建议

### 🟡 中等问题
...

### 🟢 改进建议
...

## 优点
- 列出代码中做得好的地方

## 总结
总体评价和改进优先级建议
```

## 可用模块列表
- `AI` - AI相关逻辑
- `Bean` - 数据对象/实体类
- `Common` - 公共代码
- `Component` - 组件代码
- `Enums` - 枚举定义
- `Game` - 游戏核心逻辑
- `MVC` - MVC框架代码
- `Struct` - 结构体定义
- `Utils` - 工具类